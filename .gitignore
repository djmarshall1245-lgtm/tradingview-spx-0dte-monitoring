# Local data + secrets — never commit
data/monitor.db
data/snapshots/*.json
data/shots/*.png
.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Run artifacts
*.log
payload.json

# edgar-synth repo snapshot — code only, no runtime artifacts
edgar-synth/*.db
edgar-synth/*.sqlite*
edgar-synth/data/*.db
edgar-synth/data/*.sqlite*

# uw_dashboard run artifacts — regenerated every run, never committed
data/uw_dashboard.html
data/uw_dashboard_state.json

# social_arb ntfy dedupe state — regenerated, never committed
data/social_arb_ntfy_state.json
